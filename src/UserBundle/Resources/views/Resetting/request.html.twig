
{% extends "UserBundle::layout.html.twig" %}

{% block fos_user_content %}
    {% trans_default_domain 'FOSUserBundle' %}

    <form class="form-horizontal col-md-6 col-md-offset-3" action="{{ path('fos_user_resetting_send_email') }}" method="POST">
        <div class="form-group">
            <h4>Réinitialisation de votre mot de passe</h4>

            <label for="username">{{ 'resetting.request.username'|trans }}</label>
            <input type="text" id="username" name="username" required="required" />
        </div>
        <div>
            <input class="btn btn-md btn-primary" type="submit" value="{{ 'resetting.request.submit'|trans }}" />
        </div>
    </form>
{% endblock fos_user_content %}
